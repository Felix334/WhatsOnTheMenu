Fix the Profil picture

Die AGBs als PDF speichern/herrunterladen können

Die Änderungs-Funktion is noch nicht fertig

Login/Logout-Funktion noch nicht fertig (user ID wird nicht richtig gespeichert/abgerufen)

In jedem Form ein Error handler einbauen mit der ungenutzten "formState : {error}"

Wenn das "Bild-Speicher" System fertig ist eine Möglichkeit finden den Speicher
bei Anderungen zu aktualsieren

=> In die Datenbank eine Option für Fonts einfügen

=> Ein Email-Postfach einrichten und email Adresse überall einfügen(z.B. in die AGBs)
=> Eine Email-Bestätigung für die erfolgreiche Registrierung(auch bei der Restaurant Registrierung)

Zum Schluss: https://nextjs.org/docs/app/api-reference/file-conventions/metadata/manifest
=> Und den restlichen Zusatz auch (error.js teilweise schon verhanden)

Ein Weg um die Cookies zu test (einen Knopf um sie zurück zu setzen(zum testen))
Cookies Fertg machen

=> Testen ob die Datenbank richtig diesing ist und ob alle Daten richtig abgerufen werden
(Es werden noch nicht alle Daten angezigt könnte aber an einer falschen manuellen Erstellung liegen)

Am Ende ein IP Tracking einfügen für mehr Sicherheit => (Tracking vorhanden => Datenbank anpassen)
Mit dem Dienst-Email verbinden und anzeigen wenn neue Emails angekommen sind => Wenn chat hinzugefügt dann auch

Fertige Version => Eine Kopie erstellen und Auth mit Next/Auth probieren => Testprojekt 
umwandeln


Docker funktioniert!? => Bei Code änderungen Docker-Befehl verwenden

=> Später Premium Fetaures hinzufügen(user anylytics, keine adds mehr Custom Optionen, Möglichkeit online essen zu bestellen, )